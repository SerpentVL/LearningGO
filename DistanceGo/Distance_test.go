package main

/*
func TestDistance(t *testing.T) {
	var tests = []struct {
		L    Point
		R    Point
		want float64
	}{
		{Point{0, 0}, Point{0, 0}, 0.0},
	}
	for _, test := range tests {
		if d := Distance(test.L, test.R); d != test.want {
			t.Errorf("Distance() %f != %f", d, test.want)
		}
	}
}
*/
